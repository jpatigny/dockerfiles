ARG OS_TYPE="nanoserver"
ARG OS_VERSION="20h2"

FROM mcr.microsoft.com/powershell:${OS_TYPE}-${OS_VERSION} as build
SHELL ["pwsh", "-Command", "$ErrorActionPreference = 'Stop'; $ProgressPreference = 'SilentlyContinue';"]

ARG VERSION=0.2.3

RUN iwr -uri https://github.com/hashicorp/http-echo/releases/download/v$($env:VERSION)/http-echo_$($env:VERSION)_windows_amd64.zip -OutFile http.zip -Verbose ; \
    Expand-Archive -Path http.zip -DestinationPath C:\ -Verbose ; \
    mv http-echo http-echo.exe ; \
    Remove-Item http.zip -Force 

USER       ContainerUser
ENTRYPOINT [ "C:/http-echo.exe" ]